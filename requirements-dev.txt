# Development & testing dependencies for mmpd
# Install with: pip install -r requirements-dev.txt
#
# Catatan: ini ADDITIONAL ke requirements.txt (yang berisi runtime deps).
# Untuk development lengkap: pip install -r requirements.txt -r requirements-dev.txt

# Type checking (Python <3.14 only — mypy belum support 3.14 stabil)
mypy>=1.8.0; python_version < "3.14"
types-requests>=2.31.0; python_version < "3.14"

# Testing (required, pure Python — works everywhere)
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Linting & formatting
# Catatan: ruff dan black butuh Rust toolchain untuk build dari source di
# Termux/Android. Kalau install gagal dengan error "maturin failed" atau
# "ANDROID_API_LEVEL", skip dengan: pip install --no-deps ruff black
# atau pakai pre-built wheel: pip install --only-binary :all: ruff
#
# Workaround untuk Termux:
#   export ANDROID_API_LEVEL=24
#   pkg install rust
#   pip install ruff black
#
# Atau skip ruff/black sepenuhnya (tests tetap jalan tanpa ini):
#   pip install pytest pytest-cov pytest-mock
ruff>=0.1.0; sys_platform != "android" or platform_machine != "aarch64"
black>=23.0.0; sys_platform != "android" or platform_machine != "aarch64"
