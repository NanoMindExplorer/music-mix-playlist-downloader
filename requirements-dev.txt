# Development & testing dependencies for mmpd
# Install with: pip install -r requirements-dev.txt
#
# Catatan: ini ADDITIONAL ke requirements.txt (yang berisi runtime deps).
# Untuk development lengkap: pip install -r requirements.txt -r requirements-dev.txt

# Type checking
mypy>=1.8.0
types-requests>=2.31.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Linting & formatting
ruff>=0.1.0
black>=23.0.0

# Optional: Spotify parser modernization (Fase 2.3 akan aktifkan)
# spotipy>=2.23.0
